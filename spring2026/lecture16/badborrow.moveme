fn main() {

    let mut v = vec![1, 2, 3];
    let first = &v[0];
    v.push(4);            // may reallocate the backing buffer
    println!("{first}");  // error: cannot borrow `v` as mutable
    // because it is also borrowed as immutable
}
