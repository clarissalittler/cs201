* Agenda
** Remember array addresses?
 + Which elements are next to each other in memory?
 + How far do we move when we go to the next row instead of the next column?
** The memory hierarchy
 + Registers, caches, RAM
 + Fast storage is limited
 + Temporal locality: we'll use this again soon
 + Spatial locality: we'll use something nearby soon
** Let's draw a tiny cache
 + Two sets, two integers per cache line
 + These are array indices: =0, 1, 4, 0, 1=
 + Start with an empty cache
 + Work through which accesses hit and which miss
 + Offset, set, and tag: names for the parts of the address we're using
** Does the order of a loop matter?
 + =locality.c=
 + Same array, same sum, different traversal order
 + Predict the memory accesses before timing anything
 + Check the checksums agree!
** Let's measure
 + Run several trials
 + Then =make locality-O2= and try the optimized build
 + The program alternates which traversal runs first
 + Change one thing at a time
** What did we actually measure?
 + Elapsed time vs. CPU time
 + Compiler changes, cache size, and other things running on the machine
 + We aren't guaranteed the same speedup on every computer
 + Correct result first; then find out where the time goes
** Questions / another cache trace
 + Explain why one access has locality instead of just saying which loop was faster
 + Project questions welcome
