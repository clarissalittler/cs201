* Agenda
** Assembly questions
 + What's still unclear about calls or the stack?
 + Keep the calling-convention reference open
** Using functions from another file
 + =readInt.s= and =writeInt.s=
 + Assemble each file, then link the object files together
 + =readWriteTest.s=
 + Read a number, put the result in the argument register, write it back out
 + We're using these helpers, not rewriting their parsers
** A reminder about readInt
 + Type one non-negative decimal integer, then press Enter
 + One nonempty line per call
 + This little helper doesn't validate arbitrary input or handle negative numbers
 + Don't pipe several lines into it and expect them to be read one at a time
** Turning a loop into a procedure
 + =sumLoopArray.s=
 + At the moment the sum is calculated in =_start=; only writeInt gets called
 + Let's sketch moving the sum into its own procedure
 + What are its inputs? Where does the result go?
** A couple of memory-layout details
 + Struct fields have offsets, just like array elements do
 + Padding and alignment
 + What happens if a write goes beyond the end of its buffer?
 + Use the module 4 diagrams to connect this back to addresses
** Work on the assembly assignment
 + Get one input-to-output path working first
 + Then add the interesting part of your program
 + If a call breaks things, show me what was in the registers before and after
** (If there's time) Recursion
 + =fact.s= with input 3
 + Each call gets its own saved value and return address
 + Follow the calls down, then the results back up
 + Keep inputs small; 0 through 20 fits the signed 64-bit result
