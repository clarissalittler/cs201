* Agenda
** Labels, again
 + A jump gets us somewhere
 + How do we get /back/?
 + Hardcoding a different return label every time won't get us very far
** Functions, done properly
 + =02-basic-function.s=
 + =call= saves a return address on the stack
 + =ret= takes it off and jumps back
 + Arguments in =%rdi, %rsi, %rdx, %rcx, %r8, %r9=
 + Result in =%rax=
** What survives a function call?
 + =fun1.s= and =fun2.s=
 + Write down the registers after each call
 + Both functions are allowed to change =%rdi=
 + fun2 happens to leave it alone; that isn't a promise every function makes
 + The callers differ too, so read the whole example
 + =%r9= is caller-saved as well: parking a value there doesn't save it across another call
** We need somewhere for local variables
 + =funStack.s=
 + =%rsp= points to the top of the stack
 + The stack grows toward /lower/ addresses
 + =push= and =pop=
 + =%rbp= gives this function a stable reference point for its frame
** Draw the frame
 + Return address
 + Saved old base pointer
 + Two local slots
 + Change one local value and work out the result
 + What do we restore before returning?
** Calling another function from this one
 + Keep the stack 16-byte aligned before a call
 + Save anything we'll still need afterward in the right place
 + Recursion will use exactly this machinery, repeatedly
** Questions / check your assignment's procedure arguments
 + =echo.s= and =helloRet.s= are extra syscall-result examples
 + Despite the name, helloRet doesn't contain a call/ret!
