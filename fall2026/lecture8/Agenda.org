* Agenda
** Back to C
 + Questions about the assembly assignment?
** What happens when normal control flow gets interrupted?
 + Our syscall instruction already did this
 + User mode and kernel mode
 + Switching modes doesn't necessarily mean switching to another process
 + Interrupts, traps, faults, aborts
 + The taxonomy from the course site
** What's a process?
 + An executing program and the state it needs to keep going
 + Registers, address space, open file descriptors
 + The operating system keeps track of all this
** Making another process
 + =fork_lifecycle.c=
 + One call, two paths continuing from it
 + Check what fork returns:
   + -1: something went wrong
   + 0: we're the child
   + Positive: we're the parent, and that's the child's pid
 + =fork1.c= and =fork3.c= isolate the basic idea without the complete cleanup
** What did the child actually copy?
 + Child changes its value to 99
 + Parent still has 10
 + Draw the two copies
 + Flush buffered output before forking so we're not also copying unwritten output
** Running a different program
 + =exec= replaces the program running in this process
 + It doesn't make another process
 + A successful exec doesn't return
 + Try the example's deliberate exec-failure path
** Waiting for the child
 + =waitpid= and the exit status
 + Check how the child ended before reading its exit code
 + Reaping the child / why zombies happen
** Let's draw the whole sequence
 + fork, exec, wait
 + Where could each operation fail?
 + Threads can wait until we've finished processes and signals
